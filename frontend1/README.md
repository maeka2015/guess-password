# guess-password Frontend

## Requirements

you need Node and npm. To install all th libraries required by the project run:

`npm install`

## Start server (http://localhost:3000)

`npm start`

## To run the tests

`npm test`